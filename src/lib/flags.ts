/* Presentation switches. Flip here rather than deleting markup so the design's
   guidance blocks can come back without a rebuild of the layout. */

/** "Next on requests" (dashboard), "What happens next" (request detail) and the
    classification footer note on the request form. Hidden per ACCO review. */
export const SHOW_NEXT_STEP_GUIDANCE = false;
