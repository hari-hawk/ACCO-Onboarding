/// <reference types="vite/client" />

/** Short git SHA (or Vercel commit SHA) injected by vite.config.ts. */
declare const __APP_COMMIT__: string;
/** ISO timestamp of the build. */
declare const __APP_BUILT_AT__: string;
